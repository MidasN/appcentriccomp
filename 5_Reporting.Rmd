```{r}

# install.packages('skimr')
install.packages('tidyverse')
remove.packages('tidyverse')

library(tidyselect)
library(skimr)

```


```{r}

colnames(df_clean)[-c(grep( "_timing", colnames(df_clean)))]

skim(df_clean %>% dplyr::select(colnames(df_clean)[-c(grep( "_timing", colnames(df_clean)))]))

sk %>% print.data.frame()

```
